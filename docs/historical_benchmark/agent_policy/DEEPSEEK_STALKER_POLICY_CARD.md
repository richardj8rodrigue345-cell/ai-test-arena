# DeepSeek / Stalker Policy Card

Status: draft context card.

Role:

- OpenClaw/API Stalker role.
- Paper-decision context only.

Learning method:

- Receives compact historical lessons only when the OpenClaw runner includes this card or its summary in the prompt.
- Repository presence alone is not memory.

Historical benchmark use:

- Use compact lessons from deterministic reports.
- Prefer discipline, selectivity, and clear evidence over activity.
- Treat SKIP/PASS as valid outcomes when evidence is weak.

Runtime note:

This card has no effect until a runner explicitly loads it.
