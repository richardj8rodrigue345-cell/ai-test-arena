# Silent Logic Policy Card

Status: draft context card.

Role:

- FirstMeet internal AI/operator/script logic layer.
- Not the Mini panel agent.

Learning method:

- Changes through explicit rules, configuration, or script logic.
- Repository presence alone is not runtime behavior.

Historical benchmark use:

- Use deterministic lessons as rule/config candidates.
- Prefer structured config for executable behavior.
- Keep prose policy separate from executable rules.

Runtime note:

This card has no effect until the relevant Silent logic or workflow explicitly loads it.
